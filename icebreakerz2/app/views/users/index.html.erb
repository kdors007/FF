<section class="clear">
   <h1>Welcome to IceBreakerz</h1>
   <hr>
   <br>
   <h2>Pick a game</h2>
   <br>
   <% if current_user == false%>
   <div>
      Please create an account to add content.
   </div>
   <% end %>
   	   <button id="clr" type="button" class="btn btn-primary btn-block">Would you rather...</button>
      <!-- <p> -->
      <!-- 	<%= link_to 'Would you rather...', '/posts', :id => 'wyr' %> -->	
      <!-- </p> -->

         <button id="clr" type="button" class="btn btn-primary btn-block">Never have I ever...</button>
         <!-- <p>
            <%= link_to 'Never have I ever...', '/nhieposts', :id => 'nhie'%>
            </p> -->
      </div>
</section>

<!-- WYR QUESTIONS -->
<a class="fixedbutton">Home button here</a>

<section class="clear-comment">
   <div>
      <h1>Would you rather...</h1>
   </div>
	      <hr>
   	<div class="container">
	   <div id='change'>
	      <div id='change_ques'>
	       <!--   <span>
	         <%= "#{@post.answer1}"%>
	         </span>
	         <span>
	         or
	         </span>
	         <span>
	         <%= "#{@post.answer2}" %>		
	         </span> -->
	   <br>
		<div class="btn-group">
		    <button type="button" class="btn btn-primary red_but"><%= "#{@post.answer1}"%></button>

		    <button type="button" class="btn btn-primary"><%= "#{@post.answer2}" %></button>
	  	</div>
	      </div>
	   </div>

	   <button id='new_ques'>See another random question</button>
	   <button id="home">Home</button>
   <br>
   </div>
   <hr>
   <h2>
      <%= "#{@post.comments.count}"%> Comments
   </h2>
   <%= render @post.comments%>
   <h3>
      Add a comment:
   </h3>
   <%= render "comments/form" %>
</section>
<!-- END -->

<!-- WYR INSTRUCTIONS -->
<section id='wyr_go'>
   <h1>
      Ready to play Would you Rather?
   </h1>
   <hr>
   <br>
   <div class="container">
  		<div class="jumbotron">
  		<h1>Instructions</h1>
		   <div id="instructions">
		      If you are playing this game as an, on the spur of the moment party game, then there is no need to fret, if you do not have the board game version. You can follow a simple set of would you rather game rules to play it. The rules are very basic. One player is asked a question at one time. Once they have answered the question, the rest of the players answer the same question. If the majority is in agreement with the player, they get a point. The poll about the answer to the question should be anonymous to ensure fair play. There is no winner or loser in the game. It is just a fun twist to a normal truth or dare kind of a game.
		   </div>
		  </div>
		</div>
   <div id="WYRbox">
   	  <button id="rand-q" type="button" class="btn btn-primary btn-block">Random Question</button>
      <p></p>
      <div class="alert alert-info">
      Add a Question! </div>
      <%= render "posts/form" %>
      </span>
   </div>
</section>

<!-- END -->

<section id=nhie_go>
   <div id='post'>
      <h1>
         Ready to play Never have I ever?
      </h1>
      <%= link_to "Random Question", @random_article %>
      <button id='random' href="#">Random</button>
      <h3>
         Add a Question:
      </h3>
      <%= render "nhieposts/form" %>
   </div>
</section>
<section id="nhie_post">
   <div>
      <h1> Question </h1>
   </div>
   <div>
      Never have I ever...
   </div>
   <div>
      <span class="change_nhie">
      <%= "#{@post2.question}"%>
      </span>
   </div>
   <br>
   <button id='new_nhie'>See another random question</button>
   <button id="home-nhie">Home</button>
   <%= link_to "See another random question", @random_article1 %>
   <br>
   <h2>
      <%= "#{@post2.nhiecomments.count}"%> Comments
   </h2>
   <%= render @post2.nhiecomments %>
   <h3>
      Add a comment:
   </h3>
   <%= render "nhiecomments/form" %>
</section>